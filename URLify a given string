class Solution {
    String URLify(String s) {
       String result = "";
       for(int i=0;i<s.length();i++){
           if(s.charAt(i)== ' ' ){
               result += "%20";
           }else{
               result += s.charAt(i);
           }
       }
       return result;
        
    }
}
